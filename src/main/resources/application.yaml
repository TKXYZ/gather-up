server:
  port: 9999 # localhost:9999

spring:
  application:
    name: Gather Up
  datasource:
    url: jdbc:h2:mem:mydb
    username: admin
    password: p4ssw0rd
    driverClassName: org.h2.Driver
  h2:
    console:
      enabled: true
      path: /h2
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
    # By default, the data.sql script executes before Hibernate initialization
    # The following modifies the default behavior and populates the data after the schema is generated by Hibernate
    defer-datasource-initialization: true
    properties:
      hibernate:
        format-sql: true
    hibernate:
      ddl-auto: update