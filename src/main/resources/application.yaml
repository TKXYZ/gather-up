server:
  port: 9999 # localhost:9999

spring:
  application:
    name: Gather-Up
  datasource:
    url: jdbc:h2:mem:gu-db
    username: admin
    password: p4ssw0rd
    driverClassName: org.h2.Driver
  h2:
    console:
      enabled: true
      path: /h2
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
    # By default, the data.sql script executes before Hibernate initialization
    # The following modifies the default behavior and populates the data after the schema is generated by Hibernate
    defer-datasource-initialization: true
    properties:
      hibernate:
        format-sql: true
    hibernate:
      ddl-auto: update

#spring.application.name=Gather-Up
#spring.jpa.show-sql=true
#spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
#spring.jpa.defer-datasource-initialization=true
#spring.jpa.properties.hibernate.format_sql=true
#spring.jpa.hibernate.ddl-auto=update
#spring.datasource.url=jdbc:h2:mem:gu-db
#spring.datasource.username=admin
#spring.datasource.password=p4ssw0rd
#spring.datasource.driver-class-name=org.h2.Driver
#spring.h2.console.enabled=false
#spring.h2.console.path=/h2
